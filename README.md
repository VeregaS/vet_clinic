# 🏥 Ветклиника доктора Котова

![Python](https://img.shields.io/badge/python-3.10-blue.svg)
![Django](https://img.shields.io/badge/django-4.2-green.svg)
![Docker](https://img.shields.io/badge/docker-compose-blue.svg)
![Bootstrap](https://img.shields.io/badge/bootstrap-5.3-purple.svg)

Современная система управления ветеринарной клиникой. Веб-приложение для автоматизации записи пациентов, управления расписанием и оперативного оповещения врачей.

---

## ✨ Ключевой функционал

### 👤 Для Клиентов (Публичный интерфейс)
* **Умная форма записи:** Единый интерфейс создает сразу две связанные сущности в БД — `Patient` (Пациент) и `Appointment` (Запись).
* **Защита от дублей:** Система автоматически проверяет, существует ли пациент (по имени, телефону и кличке питомца), обновляя данные или создавая новую карту.
* **Валидация данных:**
    * Блокировка записи на прошедшее время.
    * Проверка занятости по времени (защита от "двойной записи" к одному врачу).
    * Контроль временных интервалов (запись только на фиксированные слоты, например, 10:30, 11:30).
* **UX/UI:** Адаптивная верстка на Bootstrap, всплывающие уведомления.

### 👨‍⚕️ Для Персонала (Внутренний интерфейс)
пока в разработке

### 🛠 Технический стек
* **Backend:** Django, Python.
* **Database:** PostgreSQL.
* **Frontend:** Django Templates + Bootstrap 5 + Custom CSS.
* **DevOps:** Docker & Docker Compose (полная изоляция окружения, автоматический запуск БД и веб-сервера).

---

## 🚀 Быстрый старт (Installation)

Вам не нужно устанавливать Python или PostgreSQL локально. Проект полностью контейнеризирован.

### 1. Клонирование репозитория
```bash
git clone https://github.com/VeregaS/vet_clinic.git
cd vet_clinic
```

### 2. Запуск через Docker
Сборка и запуск контейнеров в фоновом режиме:
```bash
docker-compose up --build
```
После запуска сайт доступен по адресу: [http://127.0.0.1:8000](http://127.0.0.1:8000) и может быть открыт на всех утройствах в локальной сети!

### 3. Инициализация базы данных
Так как база создается с нуля, необходимо применить миграции и создать администратора:

```bash
# Применяем схему БД
docker-compose exec web python manage.py migrate

# Создаем суперпользователя для доступа в админ панель
docker-compose exec web python manage.py createsuperuser
```

---

## 📂 Структура проекта

```text
vet_clinic/
├── clinic/                  # Основное приложение Django
│   ├── migrations/          # Файлы миграций БД
│   ├── static/clinic/css/   # CSS стили и изображения
│   ├── templates/clinic/    # HTML шаблоны
│   ├── admin.py             # Админ панель
│   ├── apps.py              # Конфиг приложения
│   ├── forms.py             # Формы с кастомной валидацией
│   ├── models.py            # Модели (Doctor, Patient, Appointment)
│   ├── tests.py             # Тесты (пока пусто)
│   ├── urls.py              # Маршрутизация
│   └── views.py             # Контроллеры
├── config/                  # Глобальные настройки проекта
├── docker-compose.yml       # Конфигурация мульти-контейнерного приложения
├── Dockerfile               # Инструкции для сборки образа Python
├── .pre-commit-config.yaml  # Конфигурация контроля качества кода перед commit'ом
├── manage.py                # Утилита командной строки Django для административных задач
└── requirements.txt         # Список зависимостей  
```

---

## 🔥 Особенности реализации

1.  **Custom ModelForm Logic:**
    Используется переопределенный метод `save(commit=False)` и `get_or_create`. Это позволяет одной формой обрабатывать данные для разных таблиц базы данных, сохраняя целостность связей (Foreign Keys).

2.  **Backend Validation:**
    Валидация реализована не только на уровне HTML (атрибуты `required`), но и строго на бэкенде (метод `clean` в формах). Это предотвращает запись на занятое время, даже если запрос отправлен в обход интерфейса (например, через cURL).

---

## 🔜 Планы по развитию

* [ ] **PDF-генерация:** Автоматическое создание выписки/рецепта с логотипом клиники для печати.
* [ ] **Личный кабинет врача:** Интерфейс для заполнения истории болезни и диагнозов (мед.карта), отображение списка записей к врачу.
* [ ] **Telegram:** Создание Telegram бота для уведомления врачей о новых записях.

---

*Спасибо за внимание! :)*
