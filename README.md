# 🏥 VetClinic — Ветклиника доктора Котова

![Python](https://img.shields.io/badge/python-3.11-blue.svg)
![Django](https://img.shields.io/badge/django-5.0-green.svg)
![Docker](https://img.shields.io/badge/docker-compose-blue.svg)
![WeasyPrint](https://img.shields.io/badge/PDF-WeasyPrint-orange.svg)
![Bootstrap](https://img.shields.io/badge/UI-Bootstrap_5-purple.svg)

Это полнофункциональное веб-приложение для автоматизации работы ветеринарной клиники. Проект реализует полный цикл обслуживания: от онлайн-записи клиента до ведения медицинской карты и генерации документов в PDF.

---

## ✨ Ключевые возможности

### 👤 Публичная часть (Для клиентов)
* **Современный UI:** Адаптивный дизайн в стиле **Glassmorphism** (эффект матового стекла), реализованный на Bootstrap 5 + CSS.
* **Умная запись:**
    * Валидация времени (защита от записи в прошлое и на занятые слоты).
    * Автоматическая проверка: если клиент уже есть в базе, запись привязывается к нему; если нет — создается новый пациент.
    * Маска ввода телефона (`+7 (___) ...`) для удобства ввода.
* **Telegram-уведомления:** Врач мгновенно получает сообщение в Telegram о новой записи.

### 👨‍⚕️ (пока что не очень-то и) Закрытая часть (Для персонала)
* **Рабочее место врача (Dashboard):**
    * Просмотр расписания с фильтрацией (Все / Сегодня / Завтра).
    * Быстрое переключение между врачами (сессии).
* **Электронная медкарта (EMR):**
    * История всех визитов, диагнозов и назначений.
    * Поиск по базе пациентов (по кличке питомца, имени владельца или телефону).
* **PDF-генерация:**
    * Кнопка "Скачать карту" формирует PDF-документ с логотипом, данными пациента и историей болезней (используется библиотека `WeasyPrint`).
* **Проведение приема:**
    * Интерфейс для врача: установка статуса визита, заполнение диагноза и назначений.

---

## 🛠 Технический стек

| Категория | Технологии |
|-----------|------------|
| **Backend** | Python 3.11, Django 5.0 |
| **Database** | PostgreSQL 15 |
| **Infrastructure** | Docker, Docker Compose |
| **Frontend** | Django Templates, Bootstrap 5 |
| **PDF Engine** | WeasyPrint |
| **Code Quality** | Black, Isort, Pre-commit hooks |

---

## 🚀 Установка и запуск

Проект полностью контейнеризирован. Вам не нужно устанавливать Python или PostgreSQL локально.

### 1. Клонирование
```bash
git clone https://github.com/VeregaS/vet_clinic.git
cd vet_clinic
```

### 2. Настройка окружения (.env)
Создайте файл `.env` в корне проекта и добавьте следующие переменные:

```ini
# Настройки базы данных
DB_NAME=vetclinic_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Настройки Django
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=True

# Telegram (для уведомлений)
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
```

### 3. Запуск в Docker
Сборка и запуск контейнеров в фоновом режиме:

```bash
docker-compose up --build -d
```

После запуска сайт будет доступен по адресу: `http://0.0.0.0:8000`.

> **Примечание:** Сайт также доступен в локальной сети, если ввести **IPv4-адрес** вашего компьютера вместо `0.0.0.0` (например, `http://192.168.1.5:8000`).

### 4. Инициализация и демо-данные
Чтобы не начинать с пустой базы, выполните эти три команды по очереди. Они создадут таблицы и загрузят тестовый набор данных (Врачи, Пациенты, Записи).

```bash
# 1. Применяем структуру БД (миграции)
docker-compose exec web python manage.py migrate

# 2. Загружаем демо-данные (Врачи, Пациенты, Записи)
docker-compose exec web python manage.py loaddata initial_data

# 3. Создаем администратора (для доступа в панель управления)
docker-compose exec web python manage.py createsuperuser
```

## 🤖 Как протестировать Telegram-бота

Система умеет отправлять уведомления врачам о новых записях. Чтобы проверить это в действии:

1.  **Создайте своего бота:**
    * Напишите [@BotFather](https://t.me/BotFather) в Telegram команду `/newbot`.
    * Скопируйте полученный **Token** и вставьте его в файл `.env`:
        ```ini
        TELEGRAM_BOT_TOKEN=ваш_длинный_токен_здесь
        ```

2.  **Узнайте свой ID:**
    * Напишите боту [@userinfobot](https://t.me/userinfobot) (или любому аналогу).
    * Он пришлет ваш цифровой `ID` (например, `123456789`).

3.  **Настройте врача:**
    * Зайдите в админку: [`http://127.0.0.1:8000/admin/`](http://127.0.0.1:8000/admin/).
    * Создайте нового врача или отредактируйте существующего.
    * В поле **Telegram ID** вставьте ваш полученный ID.

4.  **Проверка:**
    * Зайдите на главную страницу как обычный клиент.
    * Запишитесь на прием именно к **этому врачу**.
    * Вы мгновенно получите уведомление в Telegram с деталями записи!

## 🔗 Навигация по проекту

После запуска сервера вам будут доступны следующие страницы:

| Страница | URL-адрес | Описание |
|---|---|---|
| **Главная (Клиент)** | [`http://127.0.0.1:8000/`](http://127.0.0.1:8000/) | Форма онлайн-записи на прием |
| **Кабинет врача** | [`http://127.0.0.1:8000/doctor/`](http://127.0.0.1:8000/doctor/) | Главное меню врача|
| **База пациентов** | [`http://127.0.0.1:8000/patients/`](http://127.0.0.1:8000/patients/) | Поиск, история болезней, PDF |
| **Админ-панель** | [`http://127.0.0.1:8000/admin/`](http://127.0.0.1:8000/admin/) | Управление БД |

---

## 📂 Структура проекта

```text
vet_clinic/
├── clinic/                  # Основное приложение Django
│   ├── fixtures/            # Тестовые данные для базы данных
│   │   └── initial_data.json
│   ├── migrations/          # Файлы миграций базы данных
│   ├── static/clinic/css/   # Статические файлы
│   │   └── style.css        # Кастомные стили (Glassmorphism)
│   ├── templates/clinic/    # HTML-шаблоны (Frontend)
│   │   ├── appointment_form.html # Форма проведения приема (для врача)
│   │   ├── base_staff.html       # Базовый шаблон для интерфейса персонала
│   │   ├── doctor_dashboard.html # Дашборд врача (расписание)
│   │   ├── home.html             # Главная страница (публичная запись)
│   │   ├── patient_detail.html   # Детальная карточка пациента + История
│   │   ├── patient_form.html     # Форма редактирования пациента
│   │   ├── patient_list.html     # База пациентов с поиском
│   │   └── patient_pdf.html      # Верстка для генерации PDF-документа
│   ├── admin.py             # Настройки админ-панели Django
│   ├── apps.py              # Конфигурация приложения
│   ├── constants.py         # Константы (слоты времени, виды животных)
│   ├── forms.py             # Формы с переопределенным методом save()
│   ├── models.py            # Модели (Appointment, Patient, Doctor)
│   ├── tests.py             # Тесты
│   ├── urls.py              # Маршрутизация внутри приложения
│   └── views.py             # Бизнес-логика (PDF, Telegram, CRUD)
├── config/                  # Глобальные настройки проекта
│   ├── asgi.py              # ASGI-конфигурация
│   ├── settings.py          # Основные настройки (БД, приложения, безопасность)
│   ├── urls.py              # Главный файл маршрутизации
│   └── wsgi.py              # WSGI-конфигурация
├── .gitignore               # Список исключений для Git
├── .pre-commit-config.yaml  # Настройки пре-коммит хуков (Black, Isort)
├── docker-compose.yml       # Оркестрация контейнеров (Web + DB)
├── Dockerfile               # Инструкции сборки образа (включая Pango/Cairo)
├── manage.py                # Утилита командной строки Django
└── requirements.txt         # Список Python-зависимостей
```
## 📂 Структура базы данных
<img width="743" height="1021" alt="image" src="https://github.com/user-attachments/assets/1b0276cd-40de-4d81-a2a7-8cf097cf3006" />

---

## 🔥 Особенности реализации

1.  **Dockerfile:**
    Сборка включает установку системных библиотек для корректной работы генератора PDF, при этом очищая кэш `apt` для минимизации размера образа.

2.  **Гибкая работа с формами:**
    В `forms.py` переопределен метод `save()`. Это позволяет одной публичной формой создавать или обновлять сразу две связанные сущности в базе данных (`Patient` и `Appointment`), сохраняя целостность данных.

3.  **Контроль качества кода:**
    В проекте настроены `pre-commit` хуки. Перед каждым коммитом код автоматически форматируется утилитой `Black` и сортирует импорты через `Isort`, что гарантирует единый стиль кода (PEP 8).

---

## 🛠 Идеи улучшений

* [x] PDF-выписки с историей болезней
* [x] Telegram-уведомления врачам
* [ ] Изменение/удаление врачей через интерфейс сайта, а не админ панель
* [ ] Авторизация врачей
* [ ] СМС/Email рассылка клиентов с напоминанием про запись
* [ ] Статистика и отчеты по пациентам


Спасибо за внимание! Хорошего дня :)
